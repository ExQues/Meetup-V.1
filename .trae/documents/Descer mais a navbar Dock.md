## Objetivo
- Posicionar o header/Dock um pouco mais abaixo na tela.

## Ajuste
- Alterar o offset do `header` de `top-4` para `top-8` em `src/App.tsx`, mantendo o `pt-24` do conteúdo para evitar sobreposição.

## Validação
- Abrir o localhost e confirmar que a navbar está visivelmente mais baixa, sem alterar o comportamento de hover.

## Arquivo
- `c:\Users\User\Desktop\PROJETOS\Meetup - Copia\src\App.tsx`